import React from 'react'
import { View, Text } from 'react-native'

import styles from './about.style'

const About = ({data, activeTab}) => {
  
  // switch(activeTab){
  //   case "Qualifications": return (data[0].job_highlights?.About??["N/A"])
  //   case "About": return (data[0].job_highlights?.About??["N/A"])
  //   case "Responsibilities": return (data[0].job_highlights?.About??["N/A"])
  //   default: break
  // }
}

export default About